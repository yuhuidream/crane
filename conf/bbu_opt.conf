# BBU config file
# SET the COMM settings in bbu.conf instead!
# This file contains all the optional configurations
# Set CONFIG_xxx to y in bbu-XX.conf file to enable according configuration

ifeq ($(CONFIG_SMP),y)
CFLAGS += -DCONFIG_SMP
endif

ifeq ($(CONFIG_QS_EXT),y)
CFLAGS += -DQ_SPY_EXT
endif

ifeq ($(CONFIG_MULTI_UART),y)
CFLAGS += -DMULTI_UART
endif

ifeq ($(CONFIG_CPU_AO),y)
CFLAGS += -DCONFIG_CPU_AO
endif

##PMIC driver support
ifeq ($(CONFIG_PMIC),y)
CFLAGS += -DCONFIG_PMIC
endif

ifeq ($(CONFIG_HWDVC),y)
CFLAGS += -DCONFIG_HWDVC
endif

ifeq ($(CONFIG_HWDFC),y)
CFLAGS += -DCONFIG_HWDFC
endif

ifeq ($(CONFIG_PPSET_CHECK),y)
CFLAGS += -DCONFIG_PPSET_CHECK
endif

ifeq ($(CONFIG_DDR_ACLK_BINDING),y)
CFLAGS += -DCONFIG_DDR_ACLK_BINDING
endif

ifeq ($(CONFIG_ETM_PTM),y)
CFLAGS += -DCONFIG_ETM_PTM
endif

ifneq ($(CONFIG_NR_CPUS),)
CFLAGS += -DCONFIG_NR_CPUS=1
endif

ifeq ($(CONFIG_TZ),y)
CFLAGS += -DCONFIG_TZ
endif

ifeq ($(CONFIG_SUSP_DBG),y)
CFLAGS += -DCONFIG_SUSP_DBG
endif

ifeq ($(CONFIG_NATO),y)
CFLAGS += -DCONFIG_NATO
ASFLAGS += -DCONFIG_NATO
endif

ifeq ($(CONFIG_BW_CTRL),y)
CFLAGS += -DQF_BANDWITH_CTRL
endif

ifeq ($(CONFIG_GENERIC_TIME),y)
CFLAGS += -DCONFIG_GENERIC_TIME
endif

ifeq ($(CONFIG_LOCAL_TIME_TICK),y)
CFLAGS += -DCONFIG_LOCAL_TIME_TICK
endif

ifeq ($(CONFIG_APB_TIME_TICK),y)
CFLAGS += -DCONFIG_APB_TIME_TICK
endif

ifeq ($(CONFIG_CP_TIME_TICK),y)
CFLAGS += -DCONFIG_CP_TIME_TICK
endif

ifeq ($(CONFIG_QK),y)
CFLAGS += -DCONFIG_QK
#ASFLAGS += -DCONFIG_QK
endif

ifeq ($(CONFIG_QS),y)
CFLAGS += -DQ_SPY
endif

ifeq ($(CONFIG_DBG),y)
CFLAGS += -DCONFIG_DBG
endif

ifeq ($(CONFIG_L2_CACHE),y)
CFLAGS += -DCONFIG_L2_CACHE
endif

ifeq ($(CONFIG_CACHE),y)
CFLAGS += -DCONFIG_CACHE
endif

ifeq ($(CONFIG_CPU_32v7),y)
CFLAGS += -D__ARM_ARCH__=7
endif

ifeq ($(CONFIG_STACK),y)
CFLAGS += -DQF_STACK_PER_AO
endif

ifeq ($(CONFIG_QH), y)
CFLAGS += -DCONFIG_QH
endif

ifeq ($(CONFIG_TMARK), y)
CFLAGS += -DCONFIG_TMARK
endif

ifeq ($(CONFIG_MOVIE_PLAYBACK),y)
CONFIG_AUDIO ?= y
CONFIG_VPU   ?= y
CONFIG_MP3_POWER_TEST ?= y
CFLAGS += -DCONFIG_MOVIE_PLAYBACK
endif

ifeq ($(CONFIG_MP3_POWER_TEST),y)
CONFIG_AUDIO ?= y
#CONFIG_VPU   ?= y
CFLAGS += -DCONFIG_MP3_POWER_TEST
endif

ifeq ($(CONFIG_AUDIO),y)
CFLAGS += -DCONFIG_AUDIO
endif

ifeq ($(CONFIG_VPU),y)
CFLAGS += -DCONFIG_VPU
#ASFLAGS += -DCONFIG_VPU
endif

ifeq ($(CONFIG_VFP),y)
CFLAGS += -DCONFIG_VFP
#ASFLAGS += -DCONFIG_VFP
endif

ifeq ($(CONFIG_OMX),y)
CFLAGS += -DCONFIG_OMX
#ASFLAGS += -DCONFIG_OMX
endif

ifeq ($(CONFIG_H264),y)
CFLAGS += -DCONFIG_H264
#ASFLAGS += -DCONFIG_H264
endif

ifeq ($(CONFIG_GCU),y)
CFLAGS += -DCONFIG_GCU
#ASFLAGS += -DCONFIG_GCU
CONFIG_MM0607 ?= y
ifeq ($(CONFIG_MM0607),y)
CFLAGS += -DCONFIG_MM0607
endif
endif

ifeq ($(CONFIG_ISP),y)
CFLAGS += -DCONFIG_ISP
#ASFLAGS += -DCONFIG_ISP
CONFIG_FVTS ?= n
ifeq ($(CONFIG_FVTS),y)
CFLAGS += -DCONFIG_FVTS
endif
CONFIG_FVTS_FS ?= n
ifeq ($(CONFIG_FVTS_FS),y)
CFLAGS += -DCONFIG_FVTS_FS
endif
endif


#Those are for automatic screen test, board test and chip test have different focus
ifeq ($(CONFIG_SCREEN_BOARD),y)
CFLAGS += -DCONFIG_SCREEN_BOARD
CFLAGS += -DCONFIG_SCREEN_CHIP
#ASFLAGS += -DCONFIG_SCREEN_BOARD
endif

ifeq ($(CONFIG_SCREEN_CHIP),y)
CFLAGS += -DCONFIG_SCREEN_CHIP
#ASFLAGS += -DCONFIG_SCREEN_CHIP
endif

#Following for ATE test status/result output selection
ifeq ($(CONFIG_ATE_EACH_STAT_RES),y)
CFLAGS += -DCONFIG_ATE_EACH_STAT_RES
endif

ifeq ($(CONFIG_ATE_CURRENT_STAT_RES),y)
CFLAGS += -DCONFIG_ATE_CURRENT_STAT_RES
endif

ifeq ($(CONFIG_ATE_GLOBAL_STAT_RES),y)
CFLAGS += -DCONFIG_ATE_GLOBAL_STAT_RES
endif

ifeq ($(CONFIG_MLUCAS),y)
CFLAGS += -DCONFIG_MLUCAS
CONFIG_MLUCAS_LIB ?= y
ifeq ($(CONFIG_MLUCAS_LIB),y)
CFLAGS += -DCONFIG_MLUCAS_LIB
endif
endif

ifeq ($(CONFIG_CHARGER),y)
CFLAGS += -DCONFIG_CHARGER
endif

ifeq ($(CONFIG_VPU_POWER_TEST),y)
CFLAGS += -DCONFIG_VPU_POWER_TEST
endif

ifeq ($(CONFIG_VIDEOPLAY_POWER_TEST),y)
CFLAGS += -DCONFIG_VIDEOPLAY_POWER_TEST
endif

ifeq ($(CONFIG_DEADIDLE_POWER_TEST),y)
CFLAGS += -DCONFIG_DEADIDLE_POWER_TEST
endif

ifeq ($(CONFIG_MEM_HOTPLUG),y)
CFLAGS += -DCONFIG_MEM_HOTPLUG
endif

ifeq ($(CONFIG_CPU_IDLE),y)
CFLAGS += -DCONFIG_CPU_IDLE
endif

ifeq ($(CONFIG_SV_MEM_TEST),y)
CFLAGS += -DSV_MEM_TEST
endif

ifeq ($(CONFIG_SIMPLE_DATA_PATTEN),y)
CFLAGS += -DSIMPLE_DATA_PATTEN
endif

# Enable load command table from eMMC
ifeq ($(CONFIG_ATE_PE_FS),y)
CFLAGS += -DCONFIG_ATE_PE_FS
#ASFLAGS += -DCONFIG_ATE_PE_FS
endif

ifeq ($(CONFIG_MMT),y)
CFLAGS += -DCONFIG_MMT
endif

ifeq ($(CONFIG_NANO_DMA),y)
CFLAGS += -DCONFIG_NANO_DMA
endif

ifeq ($(CONFIG_NANO_NOLOG),y)
CFLAGS += -DCONFIG_NANO_NOLOG
endif

ifeq ($(CONFIG_DDR_SWEEP_AUTO),y)
CFLAGS += -DDDR_SWEEP_AUTO
endif

ifeq ($(CONFIG_NANO_NO_INT),y)
CFLAGS += -DCONFIG_NANO_NO_INT
endif

ifeq ($(CONFIG_NANO_SDH),y)
CFLAGS += -DNANO_SDH
endif

ifeq ($(CONFIG_NANO_SDH_ERASE),y)
CFLAGS += -DNANO_SDH_ERASE
endif

ifeq ($(CONFIG_ATE),y)
CFLAGS += -DCONFIG_ATE
endif

ifeq ($(CONFIG_HSI),y)
CFLAGS += -DCONFIG_HSI
endif

ifeq ($(CONFIG_FAT32),y)
CFLAGS += -DCONFIG_FAT32
endif

ifeq ($(CONFIG_TCPIP),y)
CFLAGS += -DCONFIG_TCPIP
endif

ifeq ($(CONFIG_TPERF),y)
CFLAGS += -DCONFIG_TPERF
endif

ifeq ($(CONFIG_WEB_SERVER),y)
CFLAGS += -DCONFIG_WEB_SERVER
endif

ifeq ($(CONFIG_NANO_USB_SER),y)
CFLAGS += -DCONFIG_NANO_USB_SER
endif

ifeq ($(PMIC), procida)
CFLAGS += -DPMIC_PROCIDA
endif

ifeq ($(PMIC), dialog)
CFLAGS += -DPMIC_DIALOG
endif

ifeq ($(PMIC), dummy)
CFLAGS += -DPMIC_DUMMY
endif

ifeq ($(PMIC), ustica)
CFLAGS += -DPMIC_USTICA
endif

ifeq ($(CONFIG_USB3),y)
CFLAGS += -DCONFIG_USB3
endif

ifeq ($(CONFIG_USB_LINUX_MODE),y)
CFLAGS += -DCONFIG_USB_LINUX_MODE
endif

ifeq ($(CONFIG_USB_UART_DEFAULT),y)
CFLAGS += -DCONFIG_USB_UART_DEFAULT
endif

ifeq ($(CONFIG_SDH_API_TEST),y)
CFLAGS += -DCONFIG_SDH_API_TEST
endif

ifeq ($(CONFIG_FFTS),y)
CFLAGS += -DCONFIG_FFTS
endif

ifeq ($(CONFIG_NANO_BL),y)
CFLAGS += -DCONFIG_NANO_BL
endif

ifeq ($(CONFIG_MMC_CAP_HS200),y)
CFLAGS += -DMMC_CAP_HS200
endif

ifeq ($(CONFIG_PCIE),y)
CFLAGS += -DCONFIG_PCIE
endif

ifeq ($(CONFIG_NAND),y)
CFLAGS += -DCONFIG_NAND
endif

#control AP uart1/2
ifeq ($(CONFIG_CPUART),y)
CFLAGS += -DCONFIG_CPUART
endif

ifeq ($(CONFIG_UART_AP),y)
CFLAGS += -DCONFIG_UART_AP
endif

ifeq ($(CONFIG_AP_ALONE),y)
CFLAGS += -DCONFIG_AP_ALONE
endif

ifeq ($(CONFIG_CP_ALONE),y)
CFLAGS += -DCONFIG_CP_ALONE
endif

ifeq ($(CONFIG_DP_ALONE),y)
CFLAGS += -DCONFIG_DP_ALONE
endif

ifeq ($(CONFIG_SDAT),y)
CFLAGS += -DCONFIG_SDAT
endif

ifeq ($(CONFIG_SQU),y)
CFLAGS += -DCONFIG_SQU
endif
